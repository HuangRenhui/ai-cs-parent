# 开发指南

本地环境、Git 约定、工程结构。功能分期见 [功能清单](功能清单.md)；能力状态见 [模块能力](模块能力.md)。

## 📋 文档信息

- **文档版本**: v1.0
- **创建日期**: 2026-06-20
- **维护者**: huangrenhui
- **最后更新**: 2026-09-07
- **状态**: 现行

---

## 📋 目录

- [环境](#1-环境)
- [拉取与构建](#2-拉取与构建)
- [工程结构](#3-工程结构)
- [Git 约定](#4-git-约定)
- [前端与全渠道适配](#5-前端与全渠道适配)
- [调试](#6-调试)

---

## 1. 环境

- JDK 17、Maven 3.8+、Node 18+（前端 Vite 5）
- MySQL 8、Redis、Chroma/Milvus（见 [快速开始](快速开始.md)）

## 2. 拉取与构建

```bash
git clone https://gitee.com/huangrenhui/ai-cs-parent.git
cd ai-cs-parent
mvn -DskipTests package
cd ai-cs-frontend && npm install && npm run dev
```

## 3. 工程结构

见 [模块说明](模块说明.md) 与根 `pom.xml`。

## 4. Git 约定

- 分支：`main` 为主干；按 `feat(module): 中文说明` 提交
- 远程：只推 `origin`（Gitee），随后再同步 GitHub
- 不提交本地 IDE / 编辑器配置目录，提交时排除，不写入 `.gitignore`

## 5. 前端与全渠道适配

前端（`ai-cs-frontend`）为 Vue 3 + Element Plus，需同时适配 **PC / 平板 / 手机**。断点统一（`src/assets/responsive.css`）：

| 断点 | 说明 |
|------|------|
| `>1024px` | 桌面端：左侧固定菜单、多列布局 |
| `641–1024px` | 平板：窄侧栏，布局列数收紧 |
| `≤640px` | 手机：顶部抽屉菜单、单列堆叠、表格横向滚动、弹窗近全屏 |

通用规则：

- 新页面优先复用 `.page-card` / `.page-toolbar`，不要写死 `width`
- 表格依赖全局横向滚动，不要强制不换行
- 弹窗不再写死大宽度；移动端由 `responsive.css` 接管
- 新交互控件点击区 ≥ 36px；触屏时 `responsive.css` 自动放大
- 提交前至少用浏览器 DevTools 分别以手机 / 平板 / 桌面宽度走一遍

## 6. 调试

- 网关日志带 `X-Request-Id`；前端 `request.js` 已按业务码 reject
- 模型管理问题先看 [AI模型统一管理](AI模型统一管理.md)

---

## 📚 相关文档

- [快速开始](快速开始.md)
- [编码规范](编码规范.md)
- [测试指南](测试指南.md)

环境安装与服务启动见 [快速开始](快速开始.md)。本文只补充 IDE / Git 流程。

## 📋 文档信息

- **文档版本**: v1.0
- **创建日期**: 2026-06-20
- **维护者**: huangrenhui
- **最后更新**: 2026-09-05
- **状态**: 现行

---

## 📋 目录

- [工程结构](#工程结构)
- [后端开发](#后端开发)
- [前端开发](#前端开发)
- [开发流程](#开发流程)
- [CI/CD 与灰度](#cicd-与灰度)
- [常见问题](#常见问题)

---

## 工程结构

JDK / Maven / MySQL / Redis / Node 安装与验证见 [快速开始](快速开始.md)。IDE 推荐 IntelliJ IDEA。

```
ai-cs-parent/
├── ai-cs-common/          # 公共模块
├── ai-cs-api/             # Feign 客户端
├── ai-cs-gateway/         # 网关 :8080
├── ai-cs-websocket/       # WebSocket :8081
├── ai-cs-agent/           # AI 代理 :8082
├── ai-cs-knowledge/       # 知识库 :8083
├── ai-cs-base-service/    # 基础服务 :8084
├── ai-cs-workorder/       # 工单 :8085
├── ai-cs-open/            # 开放层 :8086
├── ai-cs-ops/             # 运维台 :8087
├── ai-cs-job/             # 定时任务 :8088
├── ai-cs-frontend/        # 前端 :5173
└── docs/                  # 现行手册（见 文档说明.md）
```

仓库：`git clone https://gitee.com/huangrenhui/ai-cs-parent.git`

---

## 后端开发

### 1. 初始化数据库

完整步骤见 [快速开始](快速开始.md)。不要用模板覆盖现有 `application.yml`。

```bash
mysql -u root -p < docs/database/init.sql
```

### 2. 配置

直接编辑各服务已有的 `application.yml`（库名 `ai_customer_service`）。**不要**用 `docs/配置/应用配置模板.yml` 覆盖现网配置。说明见 [配置说明](配置说明.md)。

### 3. 编译项目

```bash
# 在项目根目录执行
mvn clean install -DskipTests
```

### 4. 启动服务

**推荐启动顺序**：

顺序与端口见 [快速开始](快速开始.md)（8080–8088）。

**启动方式**：

```bash
# 方式 1: 使用 Maven
cd ai-cs-gateway
mvn spring-boot:run

# 方式 2: 使用 IDE
# 在 IntelliJ IDEA 中打开各服务的 Application 类，点击运行按钮

# 方式 3: 使用 JAR 包
cd ai-cs-gateway/target
java -jar ai-cs-gateway-1.0.0.jar
```

### 5. 验证服务

访问以下地址验证服务是否正常启动：

- 网关: http://localhost:8080/actuator/health
- 基础服务: http://localhost:8084/actuator/health
- AI 代理: http://localhost:8082/actuator/health

---

## 前端开发

### 1. 安装依赖

```bash
cd ai-cs-frontend
npm install
```

### 2. 配置 API 地址

`request.js` 使用 `baseURL: '/api'`，由 Vite 代理到网关 8080 并去掉 `/api` 前缀。不要改成直连 `http://localhost:8080/api`。

### 3. 启动开发服务器

```bash
npm run dev
```

前端应用将在 http://localhost:5173 启动（默认端口）。

### 4. 构建生产版本

```bash
npm run build
```

构建产物将输出到 `dist` 目录。

---

## 开发流程

### 分支管理策略

我们采用 Git Flow 工作流：

- **main**: 主分支，保持稳定，用于生产部署
- **develop**: 开发分支，日常开发集成
- **feature/\***: 功能分支，从 develop 分出
- **hotfix/\***: 热修复分支，从 main 分出

```bash
# 创建功能分支
git checkout develop
git checkout -b feature/your-feature-name

# 开发完成后合并回 develop
git checkout develop
git merge feature/your-feature-name
git push origin develop
```

### 代码提交规范

遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>(<scope>): <description>

[optional body]

[optional footer(s)]
```

**Type 类型**：
- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档变更
- `style`: 代码格式（不影响代码运行）
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建过程或辅助工具的变动

**示例**：

```bash
git commit -m "feat(agent): 添加对话历史记忆功能"
git commit -m "fix(knowledge): 修复向量搜索精度问题"
git commit -m "docs: 更新 API 文档"
```

### 开发新功能步骤

1. **创建功能分支**
   ```bash
   git checkout develop
   git checkout -b feature/customer-management
   ```

2. **编写代码**
   - 遵循 [编码规范](编码规范.md)
   - 编写单元测试
   - 更新相关文档

3. **本地测试**
   ```bash
   mvn test
   ```

4. **提交代码**
   ```bash
   git add .
   git commit -m "feat(base): 添加客户管理功能"
   git push origin feature/customer-management
   ```

5. **创建 Merge Request**
   - 在 GitLab 上创建 MR
   - 指定审查者
   - 等待代码审查和合并

### 调试技巧

#### 后端调试

1. **启用日志**

在 `application.yml` 中调整日志级别：

```yaml
logging:
  level:
    com.ai.cs: DEBUG
    org.springframework.web: DEBUG
```

2. **远程调试**

```bash
java -agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=5005 -jar app.jar
```

然后在 IDE 中配置 Remote Debug。

#### 前端调试

1. **浏览器开发者工具**
   - 使用 Vue Devtools 插件
   - 查看 Network 面板的 API 请求

2. **控制台日志**
   ```javascript
   console.log('Debug info:', data);
   ```

---

## CI/CD 与灰度

- **现状**: 无 GitLab CI / GitHub Actions / Jenkins；Merge Request 不会自动跑测试。
- **建议**: 最小流水线 = `mvn test` + 构建镜像；覆盖率 80% 是目标，当前未达（[测试指南](测试指南.md)）。
- **灰度**: 大改（Netty 等）见 [功能清单](功能清单.md) 第 20 节；WebSocket 未做会话同步前不要多副本灰度。
- **Nacos / K8s**: 可选，不挡单机可售闭环。

---

## 🔧 常见问题

### 1. Maven 依赖下载失败

**问题**: 依赖下载缓慢或失败

**解决方案**:

配置阿里云 Maven 镜像，编辑 `~/.m2/settings.xml`：

```xml
<mirrors>
  <mirror>
    <id>aliyun</id>
    <mirrorOf>central</mirrorOf>
    <name>Aliyun Maven Mirror</name>
    <url>https://maven.aliyun.com/repository/public</url>
  </mirror>
</mirrors>
```

### 2. 端口被占用

**问题**: 服务启动时提示端口已被占用

**解决方案**:

```bash
# Windows: 查找占用端口的进程
netstat -ano | findstr :8080
taskkill /PID <PID> /F

# Linux/Mac: 
lsof -i :8080
kill -9 <PID>
```

或者修改配置文件中的端口号。

### 3. 数据库连接失败

**问题**: 服务启动时无法连接数据库

**检查清单**:
- MySQL 服务是否启动
- 数据库是否已创建
- 用户名密码是否正确
- 防火墙是否阻止连接

### 4. Redis 连接失败

**问题**: 无法连接 Redis

**解决方案**:

```bash
# 检查 Redis 是否运行
redis-cli ping

# 应该返回 PONG
```

### 5. 前端跨域问题

**问题**: 前端调用 API 时出现 CORS 错误

**解决方案**:

网关已配置 CORS，确保前端请求通过网关（端口 8080），而不是直接访问后端服务。

### 6. Milvus 连接失败

**问题**: 知识库服务无法连接 Milvus

**解决方案**:

确保 Milvus 已启动：

```bash
# 使用 Docker 启动 Milvus
docker-compose up -d
```

---

## 📚 相关文档

- 阅读 [编码规范](编码规范.md) 了解编码约定
- 阅读 [测试指南](测试指南.md) 学习如何编写测试
- 查看 [功能清单](功能清单.md) §17-B / §18.10 再排期 CI 与灰度
- 查看 [系统架构](系统架构.md) 深入理解系统设计
- 查看 [快速开始](快速开始.md) 完成本机启动
- 查看 [文档说明](文档说明.md) 手册索引
