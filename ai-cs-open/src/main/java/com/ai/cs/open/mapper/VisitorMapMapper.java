package com.ai.cs.open.mapper;

import com.ai.cs.open.entity.VisitorMap;
import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import org.apache.ibatis.annotations.Mapper;

/**
 * 访客映射表（cs_visitor_map）Mapper，基础 CRUD 由 MyBatis-Plus 提供。
 */
@Mapper
public interface VisitorMapMapper extends BaseMapper<VisitorMap> {
}
